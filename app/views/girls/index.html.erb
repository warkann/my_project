TEST

<div style="float: right; background-color: white">
  <% if current_user %>
    <p>Привет, <%= current_user.email %></p>
    <%= link_to 'Выйти', destroy_user_session_path, :method => :delete %>
  <% else %>
    <%= link_to 'Войти на сайт', new_user_session_path %>
  <% end %>

</div>